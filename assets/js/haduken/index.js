/*
* Sample Init Neon class document ready
*/

/*$(document).ready(function(){
   window.fileBot = new  FileBot({
    data : 'this is my property',
    className : 'file-bot-wrapper'
   }).render($('body'));
});*/

$(function() {
	window.haduken = new UI.Haduken({
		data : 'this is my property',
		className : 'Haduken-wrapper'
	}).render($('body'));
});